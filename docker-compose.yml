version: '3'
services:
  db:
    container_name: hotel-db
    image: postgres:latest
    environment:
        POSTGRES_USER: program  
        POSTGRES_PASSWORD: password
        POSTGRES_DB: hotel
    ports:
        - 5432:5432